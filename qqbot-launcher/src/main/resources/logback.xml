<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- 日志存放路径 -->
    <property name="LOG_PATH" value="logs"/>

    <!-- 日志格式 -->
    <property name="CONSOLE_PATTERN"
              value="%date{HH:mm:ss.SSS} | %-5.5level | %-36.36logger{36} | %thread | %msg%n%ex{10}"/>

    <property name="ROLLING_PATTERN"
              value="%date{yyyy-MM-dd HH:mm:ss.SSS} | %-5.5level | %-36.36logger{36} | %thread | %msg%n%ex{10}"/>

    <!-- 控制台 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 滚动文件（按天 + 大小拆分） -->
    <appender name="ROLLING_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/bot.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/bot.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxHistory>7</maxHistory>
            <maxFileSize>50MB</maxFileSize>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${ROLLING_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ROLLING_FILE"/>
    </root>

    <logger name="cc.rapidev.qqbot" level="DEBUG"/>

</configuration>